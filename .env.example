# ===================================================================
# shortsgenie - AI 스포츠 영상 자동 편집
# ===================================================================
# 이 파일을 복사하여 .env 파일을 생성하세요: cp .env.example .env
# 설정 변경은 src/utils/config.py에서 하세요
# 이 파일은 API 키만 관리합니다

# ===================================================================
# Google Generative AI (Gemini) - AI 하이라이트 분석
# ===================================================================
# Google AI Studio에서 발급: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_google_api_key_here

# ===================================================================
# Groq API - 클라우드 음성 인식 (선택사항)
# ===================================================================
# Groq Console에서 발급: https://console.groq.com
# GUI에서도 입력 가능합니다
# 로컬 Whisper를 사용하려면 비워두세요
GROQ_API_KEY=

# Groq Whisper 모델 선택
# - whisper-large-v3-turbo: 가장 빠름, $0.04/hour (추천)
# - whisper-large-v3: 더 정확, $0.111/hour
GROQ_WHISPER_MODEL=whisper-large-v3-turbo

# ===================================================================
# 고급 설정 (필요시 주석 해제)
# ===================================================================
# FFmpeg 경로 (시스템 PATH에 없는 경우에만)
# FFMPEG_PATH=/path/to/ffmpeg

# ===================================================================
# 참고: 다른 모든 설정은 src/utils/config.py에서 관리됩니다
# ===================================================================
#
# 주요 설정 항목 (src/utils/config.py):
#
# [VideoConfig]
#   - output_width: 1080           # 출력 영상 폭
#   - output_height: 1920          # 출력 영상 높이 (9:16)
#   - target_fps: 30.0             # 처리 목표 fps (60fps 영상 자동 다운샘플)
#   - crf: 28                      # 영상 품질 (낮을수록 고품질)
#   - codec: "libx264"             # 비디오 코덱
#
# [DetectionConfig]
#   - model_path: "best.pt"        # YOLO 모델 (자동: resources/models/best.pt)
#   - confidence_threshold: 0.05   # 객체 탐지 신뢰도
#   - detector_backend: "yolo"     # "yolo" | "soccernet" | "footandball"
#
# [ROIConfig]
#   - ball_weight: 3.0             # 공 가중치 (ROI 계산)
#   - person_weight: 1.0           # 선수 가중치
#   - roi_scale_factor: 1.0        # ROI 확대/축소 (NOT USED, use letterbox)
#
# [CropperConfig]
#   - letterbox_top: 480           # 위쪽 검은 영역 (px)
#   - letterbox_bottom: 480        # 아래쪽 검은 영역 (px)
#
# [KalmanConfig]
#   - process_variance: 0.001      # 프로세스 분산 (카메라 반응성)
#   - measurement_variance: 0.1    # 측정 분산 (스무딩 정도)
#
# [AudioConfig]
#   - whisper_model: "base"        # tiny | base | small | medium | large
#   - whisper_device: "auto"       # auto | mps | cuda | cpu
#   - whisper_language: "ko"       # 언어 코드 (ko, en, ...)
#   - threshold_percentile: 60.0   # 하이라이트 필터 임계값
#   - merge_gap: 2.0               # 세그먼트 병합 간격 (초)
#   - segment_duration: 5.0        # 세그먼트 길이 (초)
#
# 자세한 내용: src/utils/config.py 파일 참조
